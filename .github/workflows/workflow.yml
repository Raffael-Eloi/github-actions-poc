name: GitHub Actions Workflow
on:
  push: 
  workflow_dispatch: 

jobs:
  initial:
    name: "First job"
    runs-on: ubuntu-latest
    steps:
      - name: "First action"
        run: echo "Testing"
        
  second:
    name: "Second job"
    runs-on: ubuntu-latest
    needs: initial
    steps:
      - name: "Second action"
        run: |
          echo jobs.initial.outputs
